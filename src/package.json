{
  "name": "GeoTales",
  "version": "1.0.0",
  "description": "Dev env for GeoTales",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-edit": "parcel build --public-url . --dist-dir edit/ ./edit/js/main.js",
    "build-pres": "parcel build --public-url . --dist-dir pres/ ./pres/js/main.js",
    "build": "npm run build-edit && npm run build-pres"
  },
  "author": "Andreas C. Atakan",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "mapbox-gl": "^2.11.0"
  }
}
